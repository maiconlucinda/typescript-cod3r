"use strict";
var canal = 'Graveta';
var inscrito = 610234;
// Mesmo com esse erro, por padrão a compilação será feita.
// canal = inscrito;
console.log(canal);
//! Checagem de nulo
function saudar(isManha, horas) {
    var saudacao;
    var maicon = 'Maicon';
    if (isManha) {
        saudacao = 'Bom dia!';
    }
    return maicon;
}
